# Install into a Python 3.11 venv:
#   py -3.11 -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt

basic-pitch==0.4.0
librosa==0.11.0
pretty_midi==0.2.11
numpy==1.26.4
click==8.4.1
pydantic==2.13.4
pytest==9.1.0
# PySide6 — install separately when GUI work begins:
#   .venv\Scripts\pip install PySide6
